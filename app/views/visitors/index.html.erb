<div id="wrapper">

	<!-- Sidebar -->
	<div id="sidebar-wrapper">
		<ul class="sidebar-nav">
			<li class="sidebar-brand">
				Menus
			</li>
			<% if user_signed_in?%>
			<li>
				<%= link_to 'Viagem', travels_path %>
			</li>
			<li>
				<%= link_to 'Veiculos', vehicles_path %>
			</li>
			<% if current_user.admin?%>
			<li>
				<%= link_to 'Usuarios', control_users_index_path %>
			</li>
			<% else end%>
			<li>
				<%= link_to 'Contato', contact_index_path %>
			</li>
			<% else %>
			<li>
				<%= link_to 'Contato', contact_index_path %>
			</li>
			<% end %>
		</ul>
	</div>
	<!-- /#sidebar-wrapper -->

	<!-- Page Content -->
	<div id="page-content-wrapper">
		<div class="container-fluid">
			<%if user_signed_in?%><h4 align="right"><%= current_user.email%></h4><%end%>
			<div class="row">
				<div class="col-lg-12">
					<a href="#menu-toggle" class="btn btn-default" id="menu-toggle"><span style="font-size:1.0em;" class="glyphicon glyphicon-list-alt" ></span></a>
					<h1 align="center">	R. D. V</h1>
					<h3 align="center">Relatorio de Despesas com viagens</h3>
				</br>
				</br>
				</div>
			</div>
		</div>
	</div>
	<!-- /#page-content-wrapper -->
	<p align="center">
		<span style="font-size:10.0em;" class="glyphicon glyphicon-send" ></span>
	</p>

</div>
<!-- /#wrapper -->

<!-- Menu Toggle Script -->
<script>
	$("#menu-toggle").click(function(e) {
		e.preventDefault();
		$("#wrapper").toggleClass("toggled");
	}); 
</script>
